language: generic
sudo: required
services:
  - docker

before_install: 
  - docker build -t jouiry/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true jouiry/docker-react npm run test